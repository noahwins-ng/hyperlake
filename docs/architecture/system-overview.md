# Hyperlake — System Overview

How the system actually works *now*. Kept current by `change-scope` (on scope changes) and `retro`
(against what actually shipped). If this drifts from reality it is worse than nothing.

## Architecture

<One diagram or a few lines: the major components and how data/requests flow between them.>

## Components / layers

| Layer | Responsibility |
|-------|----------------|
| <component> | <what it owns> |

## Data stores

<Databases/tables/collections and what lives in each.>

## External surfaces

<APIs, endpoints, jobs, integrations.>

## Infrastructure

<Where it runs, key services, resource limits, important env vars.>
