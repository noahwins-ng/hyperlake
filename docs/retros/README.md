# Retrospectives

One report per completed milestone, written by the `retro` skill as
`phase-<n>-<name>.md`. Each captures: timeline, what shipped, what was harder than expected,
lessons, and the invariant→guard audit.
